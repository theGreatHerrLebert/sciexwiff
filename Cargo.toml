[package]
name = "sciexwiff"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Pure-Rust reader and writer for SCIEX .wiff files (OLE2/CFB container) — no Clearcore2 SDK."
repository = "https://github.com/theGreatHerrLebert/sciexwiff"

[dependencies]
cfb = "0.10"
